#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'print the numbers'
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
int s;

for (s = 0; s < 10; s++)
	printf("%d", s);

printf("\n");

return (0);
}
